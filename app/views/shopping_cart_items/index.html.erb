<div class="contianer">
    <div class="row">
        <div class="col-md-8 col-md-offset-2">
            <h1 class="text-center">My Cart</h1>
            <% @shopping_cart_items.each do |cart_item| %>
                <div class="well">
                    <h3><%= cart_item.product_id %></h3>
                </div>
            <% end %>
        </div>
    </div>
</div>